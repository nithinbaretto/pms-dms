import type { ReactElement } from "react";
import OnboardingContainer from "./modules/onboarding/container/onboarding-container";

const App = (): ReactElement => {
	return <OnboardingContainer />;
};

export default App;
